# Deep Blue Python dependencies (Phase 4 Step 1.5)
# Install into the project-local venv: .venv\Scripts\pip install -r requirements.txt

# Core data providers
openbb>=4.0.0          # Primary provider — OpenBB Platform SDK
yfinance>=0.2.30       # Fallback provider — direct Yahoo Finance

# Data processing & storage
pandas>=2.0.0          # DataFrame manipulation
pyarrow>=14.0.0        # Parquet read/write engine

# News and web extraction
feedparser>=6.0.10
trafilatura>=1.8.0
pydantic>=2.6.0
